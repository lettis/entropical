#pragma once

namespace Transs {
  // flags for (joint) probabilities
  enum {X, X_XTAU, Y, Y_YTAU, X_Y, X_XTAU_Y, Y_YTAU_X};
  // flags for transfer entropies
  enum {XY, YX};
} // end namespace Transs

